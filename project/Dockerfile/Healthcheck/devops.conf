server {
    listen 9000;
    server_name localhost;


    # مسیر health check
    location /health-check {
        access_log off;
        return 200 'Healthy';
        add_header Content-Type text/plain;
    }
}

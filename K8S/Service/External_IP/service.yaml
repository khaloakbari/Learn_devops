apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP  # or NodePort
  externalIPs:
  - 172.19.0.100  # Arbitrary IP you control, must be routable to the cluster

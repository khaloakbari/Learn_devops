apiVersion: v1
kind: Service
metadata:
  name: external-api-service
  namespace: default
spec:
  type: ExternalName
  externalName: api.example.com # The external DNS name
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80

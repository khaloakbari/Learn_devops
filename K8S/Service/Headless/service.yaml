apiVersion: v1
kind: Service
metadata:
  name: my-db-service
spec:
  clusterIP: None  # This makes it headless
  selector:
    app: my-db
  ports:
  - protocol: TCP
    port: 3306
    targetPort: 3306
